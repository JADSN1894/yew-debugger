{
  "name": "yew-debugger",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "tailwind": "./tailwindcss -i ./input.css -o ./output.css",
    "wasm-debug": "wasm-pack build --target web --no-typescript --mode force --dev --out-name index --out-dir ../wasm ./yew-app",
    "wasm": "wasm-pack build --target web --no-typescript --mode force --release --out-name index --out-dir ../wasm ./yew-app",
    "dev": "vite",
    "debug": "npm run tailwind && npm run wasm-debug && vite build",
    "build": "npm run tailwind && npm run wasm && vite build",
    "preview": "npm run build && vite preview"
  },
  "devDependencies": {
    "@tailwindcss/typography": "0.5.10",
    "autoprefixer": "10.4.16",
    "daisyui": "3.9.3",
    "postcss": "8.4.31",
    "tailwindcss": "3.3.3",
    "vite": "4.5.0"
  }
}